import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>React Memory Game!</h1>
        <p>Click on Puzzle Piece to earn points, If you on pick the same Puzzle piece twice your points will stop!</p>
    </div>
);

export default Title;

